# .gitignore
# GitHub에 올리면 안 되는 파일들

# npm 패키지 (용량이 크고, npm install로 언제든 복원 가능)
node_modules/

# 환경변수 파일 (비밀번호, API 키 등)
.env
.env.local

# OS 자동생성 파일
.DS_Store
Thumbs.db

# VSCode 설정 (팀 프로젝트면 공유할 수도 있지만 일단 제외)
.vscode/

# 로그 파일
*.log
npm-debug.log*